//! The application's resources: DTOs returned to the frontend.
//!
//! Add one file per resource here. A resource converts a model into the JSON
//! shape the client sees, so the database schema can evolve without breaking
//! the API. The controller maps models to resources before rendering.
