{
  "name": "__PROJECT_NAME__",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "vue-tsc --noEmit",
    "dev": "node -e \"console.error('Use `arc dev`. Vite runs in middlewareMode over an IPC endpoint so this project has exactly one TCP port; `vite dev` would open a second one.'); process.exit(1)\""
  },
  "dependencies": {
    "@inertiajs/vue3": "^3.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vue-tsc": "^3.0.0"
  }
}
